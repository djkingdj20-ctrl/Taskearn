require("dotenv").config();const express=require("express");const session=require("express-session");const path=require("path");const fs=require("fs");const crypto=require("crypto");const bcrypt=require("bcryptjs");const {google}=require("googleapis");const app=express();app.set("trust proxy",1);app.use(express.json({limit:"5mb"}));app.use(express.urlencoded({extended:true,limit:"100kb"}));const PORT=Number(process.env.PORT)||10000;const DATA_DIR=path.join(__dirname,"data");const DB_FILE=path.join(DATA_DIR,"database.json");const SESSION_SECRET=String(process.env.SESSION_SECRET||"").trim()||"CHANGE_THIS_TASKEARN_SECRET_2026";const GMAIL_USER=String(process.env.GMAIL_USER||"").trim();const EMAIL_FROM=String(process.env.EMAIL_FROM||GMAIL_USER).trim();const GOOGLE_CLIENT_ID=String(process.env.GOOGLE_CLIENT_ID||"").trim();const GOOGLE_CLIENT_SECRET=String(process.env.GOOGLE_CLIENT_SECRET||"").trim();const GOOGLE_REFRESH_TOKEN=String(process.env.GOOGLE_REFRESH_TOKEN||"").trim();const GOOGLE_CLIENT_ID_FOR_LOGIN=GOOGLE_CLIENT_ID;if(!fs.existsSync(DATA_DIR))fs.mkdirSync(DATA_DIR,{recursive:true});let gmailClient=null;let gmailApiConfigured=false;function configureGmailApi(){if(!GMAIL_USER||!GOOGLE_CLIENT_ID||!GOOGLE_CLIENT_SECRET||!GOOGLE_REFRESH_TOKEN){console.log("Gmail API: NOT CONFIGURED");gmailApiConfigured=false;gmailClient=null;return}try{const oauth2Client=new google.auth.OAuth2(GOOGLE_CLIENT_ID,GOOGLE_CLIENT_SECRET,"https://developers.google.com/oauthplayground");oauth2Client.setCredentials({refresh_token:GOOGLE_REFRESH_TOKEN});gmailClient=google.gmail({version:"v1",auth:oauth2Client});gmailApiConfigured=true;console.log("Gmail API: CONFIGURED")}catch(error){gmailApiConfigured=false;gmailClient=null;console.error("Gmail API configuration error:",error.message||error)}}configureGmailApi();function defaultDatabase(){return{users:[],tasks:[{id:1,title:"Complete a simple online task",description:"Complete the instructions carefully and submit the task for review.",type:"General",reward:10,active:true},{id:2,title:"Social Media Engagement",description:"Complete the specified social media activity and submit your task.",type:"Social",reward:15,active:true},{id:3,title:"Website Visit Task",description:"Visit the required website and complete the provided instructions.",type:"Website",reward:20,active:true},{id:4,title:"Watch Ad",description:"Watch one rewarded ad and earn 10 TaskEarn Points.",type:"Rewarded Ad",reward:10,active:true,rewardType:"ad",dailyLimit:10}],submissions:[],withdrawals:[],otpCodes:[],rewardHistory:[]}}function saveDB(database){const tempFile=DB_FILE+".tmp";fs.writeFileSync(tempFile,JSON.stringify(database,null,2),"utf8");fs.renameSync(tempFile,DB_FILE)}function loadDB(){try{if(!fs.existsSync(DB_FILE)){const database=defaultDatabase();saveDB(database);return database}const raw=fs.readFileSync(DB_FILE,"utf8");if(!raw.trim()){const database=defaultDatabase();saveDB(database);return database}const database=JSON.parse(raw);database.users||=[];database.tasks||=[];database.submissions||=[];database.withdrawals||=[];database.otpCodes||=[];database.rewardHistory||=[];if(!database.tasks.some(t=>t.rewardType==="ad"))database.tasks.push({id:4,title:"Watch Ad",description:"Watch one rewarded ad and earn 10 TaskEarn Points.",type:"Rewarded Ad",reward:10,active:true,rewardType:"ad",dailyLimit:10});database.users.forEach(user=>{user.mobile??="";user.city??="";user.profileImage??="";user.emailVerified??=false;user.mobileVerified??=false;user.googleId??="";user.authProvider??=user.googleId?"google":"local";user.points??=Number(user.balance||0);user.balance=0;user.role??="Member";user.passwordHash??=""});return database}catch(error){console.error("Database error:",error);const database=defaultDatabase();saveDB(database);return database}}let db=loadDB();app.use(session({secret:SESSION_SECRET,resave:false,saveUninitialized:false,cookie:{httpOnly:true,sameSite:"lax",secure:process.env.NODE_ENV==="production",maxAge:1000*60*60*24*30}}));function cleanText(value,max=200){return String(value??"").trim().slice(0,max)}function normalizeEmail(value){return cleanText(value,160).toLowerCase()}function normalizeMobile(value){return cleanText(value,20).replace(/[\s()-]/g,"")}function validEmail(email){return/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email)}function validMobile(mobile){return/^\+?[0-9]{7,15}$/.test(mobile)}function publicUser(user){if(!user)return null;return{id:user.id,name:user.name||"",email:user.email||"",role:user.role||"Member",mobile:user.mobile||"",city:user.city||"",profileImage:user.profileImage||"",emailVerified:Boolean(user.emailVerified),mobileVerified:Boolean(user.mobileVerified),authProvider:user.authProvider||"local",points:Number(user.points||0),createdAt:user.createdAt}}function findUserById(id){return db.users.find(user=>String(user.id)===String(id))}function currentUser(req){return req.session.userId?findUserById(req.session.userId):null}function requireLogin(req,res,next){const user=currentUser(req);if(!user)return res.status(401).json({error:"Please login first."});req.user=user;next()}const OTP_EXPIRY_MS=10*60*1000;const OTP_RESEND_MS=60*1000;const OTP_MAX_ATTEMPTS=5;function randomOtp(){return String(crypto.randomInt(100000,1000000))}function hashOtp(otp){return crypto.createHash("sha256").update(String(otp)).digest("hex")}function safeCompare(a,b){const aa=Buffer.from(String(a));const bb=Buffer.from(String(b));if(aa.length!==bb.length)return false;return crypto.timingSafeEqual(aa,bb)}function createOtpRecord(user,type,purpose){const otp=randomOtp();const now=Date.now();db.otpCodes=db.otpCodes.filter(record=>!(String(record.userId)===String(user.id)&&record.type===type&&record.purpose===purpose));db.otpCodes.push({userId:user.id,type,purpose,otpHash:hashOtp(otp),createdAt:new Date(now).toISOString(),expiresAt:new Date(now+OTP_EXPIRY_MS).toISOString(),attempts:0,lastSentAt:new Date(now).toISOString()});saveDB(db);return otp}function getOtpRecord(userId,type,purpose){return db.otpCodes.filter(record=>String(record.userId)===String(userId)&&record.type===type&&record.purpose===purpose).sort((a,b)=>new Date(b.createdAt)-new Date(a.createdAt))[0]}function deleteOtpRecord(userId,type,purpose){db.otpCodes=db.otpCodes.filter(record=>!(String(record.userId)===String(userId)&&record.type===type&&record.purpose===purpose));saveDB(db)}function createRawEmail({from,to,subject,text,html}){const boundary="TaskEarnBoundary"+crypto.randomBytes(16).toString("hex");const safeFrom=String(from).replace(/[\r\n]/g,"");const safeTo=String(to).replace(/[\r\n]/g,"");const safeSubject=String(subject).replace(/[\r\n]/g," ");const message=[`From: ${safeFrom}`,`To: ${safeTo}`,`Subject: ${safeSubject}`,"MIME-Version: 1.0",`Content-Type: multipart/alternative; boundary="${boundary}"`,"",`--${boundary}`,'Content-Type: text/plain; charset="UTF-8"',"Content-Transfer-Encoding: 8bit","",text,"",`--${boundary}`,'Content-Type: text/html; charset="UTF-8"',"Content-Transfer-Encoding: 8bit","",html,"",`--${boundary}--`].join("\r\n");return Buffer.from(message,"utf8").toString("base64").replace(/\+/g,"-").replace(/\//g,"_").replace(/=+$/,"")}async function sendEmailOtp(email,otp){if(!gmailApiConfigured||!gmailClient)throw new Error("Gmail API is not configured. Check GMAIL_USER, GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET and GOOGLE_REFRESH_TOKEN.");const text=`Your TaskEarn verification code is ${otp}. This OTP expires in 10 minutes.\n\nIf you did not request this code, please ignore this email.`;const html=`<!DOCTYPE html><html><head><meta charset="UTF-8"><title>TaskEarn OTP</title></head><body style="margin:0;padding:0;background:#f5f7fb;font-family:Arial,Helvetica,sans-serif"><div style="max-width:600px;margin:30px auto;background:#fff;border-radius:16px;padding:30px;box-shadow:0 4px 20px rgba(0,0,0,.08)"><h1 style="color:#ff5d2e">TaskEarn</h1><h2>Email Verification</h2><p>Hello,</p><p>Your TaskEarn verification code is:</p><div style="font-size:36px;font-weight:bold;letter-spacing:10px;text-align:center;background:#f1f3f5;padding:22px;border-radius:12px;margin:25px 0">${otp}</div><p>This OTP expires in <strong>10 minutes</strong>.</p><p>If you did not request this code, please ignore this email.</p><p style="margin-top:30px;color:#777;font-size:13px">TaskEarn</p></div></body></html>`;const raw=createRawEmail({from:EMAIL_FROM||GMAIL_USER,to:email,subject:"TaskEarn Email Verification Code",text,html});try{return(await gmailClient.users.messages.send({userId:"me",requestBody:{raw}})).data}catch(error){console.error("Gmail API send error:",error?.response?.data||error?.message||error);throw new Error("Unable to send Gmail OTP. Please check Gmail API OAuth configuration and GOOGLE_REFRESH_TOKEN.")}}async function sendSmsOtp(mobile,otp){const sid=process.env.TWILIO_ACCOUNT_SID;const token=process.env.TWILIO_AUTH_TOKEN;const from=process.env.TWILIO_PHONE_NUMBER;if(!sid||!token||!from)throw new Error("Mobile verification service is not configured.");if(typeof fetch!=="function")throw new Error("This server requires Node.js 18 or newer.");const auth=Buffer.from(`${sid}:${token}`).toString("base64");const params=new URLSearchParams();params.append("To",mobile);params.append("From",from);params.append("Body",`TaskEarn verification code: ${otp}. This code expires in 10 minutes.`);const response=await fetch(`https://api.twilio.com/2010-04-01/Accounts/${encodeURIComponent(sid)}/Messages.json`,{method:"POST",headers:{Authorization:`Basic ${auth}`,"Content-Type":"application/x-www-form-urlencoded"},body:params.toString()});if(!response.ok){console.error("Twilio error:",await response.text());throw new Error("Unable to send mobile verification OTP.")}}async function sendOtpFor(user,type,purpose,force=false){const existing=getOtpRecord(user.id,type,purpose);if(existing&&!force){const elapsed=Date.now()-new Date(existing.lastSentAt).getTime();if(elapsed<OTP_RESEND_MS)throw new Error(`Please wait ${Math.ceil((OTP_RESEND_MS-elapsed)/1000)} seconds before requesting another OTP.`)}const otp=createOtpRecord(user,type,purpose);try{if(type==="email")await sendEmailOtp(user.email,otp);else if(type==="mobile")await sendSmsOtp(user.mobile,otp);else throw new Error("Invalid OTP type.")}catch(error){deleteOtpRecord(user.id,type,purpose);throw error}}function completeLogin(req,user,remember){req.session.userId=user.id;delete req.session.verification;delete req.session.withdrawalVerification;req.session.cookie.maxAge=remember?1000*60*60*24*30:1000*60*60*12}async function verifyGoogleIdToken(idToken){if(!GOOGLE_CLIENT_ID_FOR_LOGIN)throw new Error("Google Sign-In is not configured.");if(!idToken)throw new Error("Google authentication token is missing.");const response=await fetch("https://oauth2.googleapis.com/tokeninfo?id_token="+encodeURIComponent(idToken));if(!response.ok)throw new Error("Invalid Google authentication token.");const data=await response.json();if(data.aud!==GOOGLE_CLIENT_ID_FOR_LOGIN)throw new Error("Google client ID does not match.");if(data.email_verified!==true&&data.email_verified!=="true")throw new Error("Your Google email is not verified.");return{googleId:cleanText(data.sub,200),email:normalizeEmail(data.email),name:cleanText(data.name||data.email.split("@")[0],80),picture:cleanText(data.picture||"",1000)}}app.get("/api/google-config",(req,res)=>res.json({clientId:GOOGLE_CLIENT_ID}));app.post("/api/auth/google",async(req,res)=>{try{const googleUser=await verifyGoogleIdToken(req.body.credential);let user=db.users.find(u=>u.googleId&&String(u.googleId)===String(googleUser.googleId));if(!user)user=db.users.find(u=>normalizeEmail(u.email)===googleUser.email);if(!user){user={id:crypto.randomUUID(),name:googleUser.name,email:googleUser.email,passwordHash:"",role:"Member",mobile:"",city:"",profileImage:googleUser.picture,emailVerified:true,mobileVerified:false,googleId:googleUser.googleId,authProvider:"google",points:0,balance:0,createdAt:new Date().toISOString()};db.users.push(user)}else{user.googleId=googleUser.googleId;user.emailVerified=true;user.authProvider="google";if(!user.profileImage)user.profileImage=googleUser.picture;if(!user.name)user.name=googleUser.name}saveDB(db);completeLogin(req,user,true);res.json({success:true,user:publicUser(user)})}catch(error){res.status(401).json({error:error.message||"Google login failed."})}});app.get("/",(req,res)=>res.sendFile(path.join(__dirname,"public","index.html")));app.use(express.static(path.join(__dirname,"public")));app.post("/api/register",async(req,res)=>{try{const name=cleanText(req.body.name,80);const email=normalizeEmail(req.body.email);const mobile=normalizeMobile(req.body.mobile);const city=cleanText(req.body.city,80);const password=String(req.body.password||"");if(!name||!email||!mobile||!city||!password)return res.status(400).json({error:"Please complete all fields."});if(!validEmail(email))return res.status(400).json({error:"Please enter a valid email address."});if(!validMobile(mobile))return res.status(400).json({error:"Please enter a valid mobile number."});if(password.length<6)return res.status(400).json({error:"Password must contain at least 6 characters."});if(db.users.some(u=>normalizeEmail(u.email)===email))return res.status(409).json({error:"An account with this email already exists. Please login."});if(db.users.some(u=>u.mobile===mobile))return res.status(409).json({error:"This mobile number is already registered."});const user={id:crypto.randomUUID(),name,email,passwordHash:await bcrypt.hash(password,12),role:"Member",mobile,city,profileImage:"",emailVerified:false,mobileVerified:false,googleId:"",authProvider:"local",points:0,balance:0,createdAt:new Date().toISOString()};db.users.push(user);saveDB(db);req.session.verification={userId:user.id,purpose:"register",step:"email"};try{await sendOtpFor(user,"email","register",true)}catch(error){db.users=db.users.filter(u=>String(u.id)!==String(user.id));saveDB(db);throw error}const record=getOtpRecord(user.id,"email","register");res.json({verificationRequired:true,step:"email",email:user.email,mobile:user.mobile,expiresAt:record?.expiresAt||null,resendAt:record?new Date(new Date(record.lastSentAt).getTime()+OTP_RESEND_MS).toISOString():null})}catch(error){res.status(500).json({error:error.message||"Unable to create account."})}});app.post("/api/login",async(req,res)=>{try{const email=normalizeEmail(req.body.email);const password=String(req.body.password||"");const remember=Boolean(req.body.remember);const user=db.users.find(u=>normalizeEmail(u.email)===email);if(!user||!user.passwordHash)return res.status(401).json({error:"Invalid email or password. If you registered with Google, use Google Sign-In."});if(!(await bcrypt.compare(password,user.passwordHash)))return res.status(401).json({error:"Invalid email or password."});if(!user.emailVerified){req.session.verification={userId:user.id,purpose:"login",step:"email"};await sendOtpFor(user,"email","login",true);const record=getOtpRecord(user.id,"email","login");return res.json({verificationRequired:true,step:"email",email:user.email,mobile:user.mobile,expiresAt:record?.expiresAt||null,resendAt:record?new Date(new Date(record.lastSentAt).getTime()+OTP_RESEND_MS).toISOString():null})}completeLogin(req,user,remember);res.json({user:publicUser(user)})}catch(error){res.status(500).json({error:error.message||"Unable to login."})}});app.post("/api/verify-otp",async(req,res)=>{try{const verification=req.session.verification;if(!verification)return res.status(400).json({error:"Verification session expired. Please login again."});const user=findUserById(verification.userId);if(!user)return res.status(404).json({error:"Account not found."});const otp=cleanText(req.body.otp,6);if(!/^\d{6}$/.test(otp))return res.status(400).json({error:"Please enter the 6-digit OTP."});const record=getOtpRecord(user.id,"email",verification.purpose);if(!record)return res.status(400).json({error:"OTP not found. Please request a new OTP."});if(Date.now()>new Date(record.expiresAt).getTime()){deleteOtpRecord(user.id,"email",verification.purpose);return res.status(400).json({error:"OTP has expired. Please click Resend OTP."})}if(record.attempts>=OTP_MAX_ATTEMPTS){deleteOtpRecord(user.id,"email",verification.purpose);return res.status(429).json({error:"Too many incorrect attempts. Please click Resend OTP."})}if(!safeCompare(record.otpHash,hashOtp(otp))){record.attempts++;saveDB(db);return res.status(401).json({error:"Incorrect OTP."})}deleteOtpRecord(user.id,"email",verification.purpose);user.emailVerified=true;saveDB(db);completeLogin(req,user,true);res.json({step:"complete",user:publicUser(user)})}catch(error){res.status(500).json({error:error.message||"Unable to verify OTP."})}});app.post("/api/resend-otp",async(req,res)=>{try{const verification=req.session.verification;if(!verification)return res.status(400).json({error:"Verification session expired. Please login again."});const user=findUserById(verification.userId);if(!user)return res.status(404).json({error:"Account not found."});await sendOtpFor(user,"email",verification.purpose,false);const record=getOtpRecord(user.id,"email",verification.purpose);res.json({success:true,message:"A new Gmail OTP has been sent.",expiresAt:record?.expiresAt||null,resendAt:record?new Date(new Date(record.lastSentAt).getTime()+OTP_RESEND_MS).toISOString():null})}catch(error){res.status(400).json({error:error.message||"Unable to resend OTP."})}});app.get("/api/otp-status",(req,res)=>{const verification=req.session.verification;if(!verification)return res.json({active:false});const user=findUserById(verification.userId);if(!user)return res.json({active:false});const record=getOtpRecord(user.id,"email",verification.purpose);if(!record)return res.json({active:false});const now=Date.now();const expiresAt=new Date(record.expiresAt).getTime();const resendAt=new Date(record.lastSentAt).getTime()+OTP_RESEND_MS;res.json({active:expiresAt>now,expiresAt:record.expiresAt,resendAt:new Date(resendAt).toISOString(),expiresIn:Math.max(0,Math.ceil((expiresAt-now)/1000)),resendIn:Math.max(0,Math.ceil((resendAt-now)/1000))})});app.get("/api/me",(req,res)=>res.json(publicUser(currentUser(req))));app.post("/api/logout",(req,res)=>req.session.destroy(error=>{if(error)return res.status(500).json({error:"Unable to logout."});res.clearCookie("connect.sid");res.json({message:"Logged out successfully."})}));app.put("/api/profile",requireLogin,(req,res)=>{try{const user=req.user;const name=cleanText(req.body.name,80);const mobile=normalizeMobile(req.body.mobile);const city=cleanText(req.body.city,80);if(!name||!city)return res.status(400).json({error:"Name and city are required."});if(mobile&&!validMobile(mobile))return res.status(400).json({error:"Please enter a valid mobile number."});if(mobile!==user.mobile){if(mobile&&db.users.find(u=>String(u.id)!==String(user.id)&&u.mobile===mobile))return res.status(409).json({error:"This mobile number is already registered."});user.mobile=mobile;user.mobileVerified=false}user.name=name;user.city=city;saveDB(db);res.json({message:"Profile saved successfully.",user:publicUser(user)})}catch(error){res.status(500).json({error:"Unable to save profile."})}});app.get("/api/tasks",requireLogin,(req,res)=>res.json(db.tasks.filter(t=>t.active!==false).map(t=>({id:t.id,title:t.title,description:t.description,type:t.type,reward:Number(t.reward)||0,rewardType:t.rewardType||null,dailyLimit:Number(t.dailyLimit)||null}))));app.post("/api/tasks/:id/submit",requireLogin,(req,res)=>{const taskId=Number(req.params.id);const task=db.tasks.find(t=>Number(t.id)===taskId&&t.active!==false);if(!task)return res.status(404).json({error:"Task not found."});if(task.rewardType==="ad")return res.status(400).json({error:"Rewarded ad tasks must be completed through the rewarded-ad flow."});const existing=db.submissions.find(s=>String(s.userId)===String(req.user.id)&&Number(s.taskId)===taskId&&s.status==="pending");if(existing)return res.status(409).json({error:"You already submitted this task and it is under review."});db.submissions.push({id:crypto.randomUUID(),userId:req.user.id,taskId:task.id,taskTitle:task.title,reward:Number(task.reward)||0,status:"pending",submittedAt:new Date().toISOString()});saveDB(db);res.json({message:"Task submitted for review successfully."})});app.get("/api/my-submissions",requireLogin,(req,res)=>res.json(db.submissions.filter(s=>String(s.userId)===String(req.user.id)).sort((a,b)=>new Date(b.submittedAt)-new Date(a.submittedAt))));function dayStart(){const d=new Date();d.setHours(0,0,0,0);return d.getTime()}function getTodayAdClaims(userId){return db.rewardHistory.filter(r=>String(r.userId)===String(userId)&&r.rewardType==="ad"&&r.status==="credited"&&new Date(r.createdAt).getTime()>=dayStart())}app.get("/api/points",requireLogin,(req,res)=>res.json({points:Number(req.user.points||0),todayAdClaims:getTodayAdClaims(req.user.id).length,dailyAdLimit:10}));app.get("/api/rewards/history",requireLogin,(req,res)=>res.json(db.rewardHistory.filter(r=>String(r.userId)===String(req.user.id)).sort((a,b)=>new Date(b.createdAt)-new Date(a.createdAt))));app.post("/api/rewards/ad-complete",requireLogin,(req,res)=>{try{const user=req.user;const taskId=Number(req.body.taskId);const adEventId=cleanText(req.body.adEventId,200);const rewardToken=cleanText(req.body.rewardToken,500);if(!Number.isInteger(taskId))return res.status(400).json({error:"Invalid reward task."});const task=db.tasks.find(t=>Number(t.id)===taskId&&t.active!==false&&t.rewardType==="ad");if(!task)return res.status(404).json({error:"Rewarded ad task not found."});if(!adEventId)return res.status(400).json({error:"A valid ad completion event is required."});if(!rewardToken)return res.status(400).json({error:"A valid ad reward confirmation is required."});if(db.rewardHistory.some(r=>r.adEventId===adEventId))return res.status(409).json({error:"This reward has already been claimed."});const todayClaims=getTodayAdClaims(user.id);const dailyLimit=Number(task.dailyLimit)||10;if(todayClaims.length>=dailyLimit)return res.status(429).json({error:`Daily rewarded-ad limit reached. Maximum ${dailyLimit} ads per day.`});const lastClaim=todayClaims[todayClaims.length-1];if(lastClaim&&Date.now()-new Date(lastClaim.createdAt).getTime()<5000)return res.status(429).json({error:"Please wait before claiming another reward."});const points=Math.max(0,Number(task.reward)||10);const reward={id:crypto.randomUUID(),userId:user.id,taskId:task.id,rewardType:"ad",points,adEventId,createdAt:new Date().toISOString(),status:"credited"};db.rewardHistory.push(reward);user.points=Number(user.points||0)+points;saveDB(db);res.json({success:true,message:"Ad completed successfully.",pointsAdded:points,points:Number(user.points||0),rewardId:reward.id})}catch(error){console.error("Ad reward error:",error);res.status(500).json({error:"Unable to confirm ad reward."})}});app.get("/api/wallet",requireLogin,(req,res)=>res.json({points:Number(req.user.points||0),cashWithdrawalDisabled:true,message:"TaskEarn Points are non-transferable and cannot currently be withdrawn as cash."}));app.post("/api/withdrawal/send-mobile-otp",requireLogin,(req,res)=>res.status(403).json({error:"Cash withdrawals are disabled for TaskEarn Points."}));app.post("/api/withdrawal/verify-mobile-otp",requireLogin,(req,res)=>res.status(403).json({error:"Cash withdrawals are disabled for TaskEarn Points."}));app.post("/api/withdrawal/resend-mobile-otp",requireLogin,(req,res)=>res.status(403).json({error:"Cash withdrawals are disabled for TaskEarn Points."}));app.post("/api/withdraw",requireLogin,(req,res)=>res.status(403).json({error:"Cash withdrawals are disabled. TaskEarn Points are non-transferable and cannot be directly converted to cash."}));app.get("/api/health",(req,res)=>res.json({status:"ok",service:"TaskEarn",time:new Date().toISOString(),gmailConfigured:Boolean(GMAIL_USER),gmailApiConfigured:Boolean(gmailApiConfigured),googleConfigured:Boolean(GOOGLE_CLIENT_ID),googleClientSecretConfigured:Boolean(GOOGLE_CLIENT_SECRET),googleRefreshTokenConfigured:Boolean(GOOGLE_REFRESH_TOKEN),twilioConfigured:Boolean(process.env.TWILIO_ACCOUNT_SID&&process.env.TWILIO_AUTH_TOKEN&&process.env.TWILIO_PHONE_NUMBER),emailProvider:"Gmail API",rewardSystem:"TaskEarn Points",cashWithdrawal:false}));app.use("/api",(req,res)=>res.status(404).json({error:"API endpoint not found."}));app.use((err,req,res,next)=>{console.error("Server error:",err);if(res.headersSent)return next(err);res.status(500).json({error:"Internal server error."})});app.listen(PORT,"0.0.0.0",()=>{console.log(`TaskEarn server running on port ${PORT}`);console.log(`Gmail API configured: ${gmailApiConfigured?"YES":"NO"}`);console.log(`TaskEarn Points reward system: ENABLED`);console.log(`Cash withdrawal: DISABLED`)});
